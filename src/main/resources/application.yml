spring:
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/clinica
    username: postgres
    password: matias1234
    driverClassName: org.postgresql.Driver